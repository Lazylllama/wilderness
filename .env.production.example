# Postgres password for the bundled db service (DATABASE_URL is built from it).
POSTGRES_PASSWORD=

# Public URL of the app. Point your proxy (Nest, nginx, Caddy) at APP_PORT.
APP_URL=https://wilderness.example.com
APP_PORT=3000

# Only if NOTHING terminates TLS in front of you: Thruster fetches its own
# Let's Encrypt cert for this domain (and you map 80/443 in compose.prod.yml).
# TLS_DOMAIN=wilderness.example.com

# bin/rails secret
SECRET_KEY_BASE=

# bin/rails db:encryption:init (keep these stable or encrypted columns are lost)
AR_ENCRYPTION_PRIMARY_KEY=
AR_ENCRYPTION_DETERMINISTIC_KEY=
AR_ENCRYPTION_KEY_DERIVATION_SALT=

# Hack Club oauth app creds
HACKCLUB_CLIENT_ID=
HACKCLUB_CLIENT_SECRET=
